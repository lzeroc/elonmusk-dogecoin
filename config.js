var search = [ // search keyword
    "doge",
    "dogecoin"
];

var binance = {
    apiKey: '', // binance apiKey
    secret: '', // binance secret
}

var amount = 1000; // buy doge

module.exports = {
    search,
    binance,
    amount,
}
