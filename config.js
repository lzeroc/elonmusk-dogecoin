var search = [ // search keyword
    "doge",
    "dogecoin"
];

var apiKey = ''; // binance apiKey
var secret = ''; // binance secret

var amount = 1000; // buy doge

module.exports = {
    search,
    apiKey,
    secret,
    amount,
}
